# Everything the e2e suite needs on top of the shared repo requirements.
# Install via install-deps.sh (CI does too), not bare pip: these pins pull in
# conflicting providers of the `scalecodec` namespace that the script reorders.
-r ../requirements.txt
bittensor-cli==9.23.2
substrate-interface==1.7.11
