name: CI

permissions:
  contents: read

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'push' && github.sha || github.ref }}
  cancel-in-progress: true

env:
  FOUNDRY_PROFILE: ci

jobs:
  check:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.7.0

      - name: Cache forge artifacts
        uses: actions/cache@v5
        with:
          path: |
            out
            cache
          key: forge-${{ runner.os }}-ci-${{ hashFiles('foundry.toml', 'src/**/*.sol', 'test/**/*.sol', 'lib/**') }}
          restore-keys: forge-${{ runner.os }}-ci-

      - name: Run Forge fmt
        run: forge fmt --check

      - name: Run Forge build
        run: |
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          forge build --sizes | tee -a "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      # Fuzz input streams depend on test-runner thread scheduling, so pin the thread
      # count; regenerate .gas-snapshot locally with the same --threads value.
      - name: Run tests and check gas snapshot
        run: forge snapshot --check --tolerance 1 -vvv --threads 4

      - name: Check per-call gas snapshots have not drifted
        run: git diff --exit-code snapshots/
